const Stats = ({ totalCount }) => {
	return (
		<div className="text-gray-600 px-10 py-3 rounded-xl bg-gray-100 text-3xl font-bold">
			Total Count: {totalCount}
		</div>
	);
};

export default Stats;
